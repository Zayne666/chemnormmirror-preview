const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));(async()=>{const q=new URLSearchParams(location.search),key=q.get('key'),name=q.get('product'),identifier=q.get('identifier'),summary=document.getElementById('summary');summary.innerHTML=`<span>Product</span><b>${esc(name||'General product inquiry')}</b>${identifier?`<small>${esc(identifier)}</small>`:''}`;try{const data=await(await fetch('data/chemnorm/products.json')).json(),current=data.find(x=>String(x.product_no||'')===key)||data.find(x=>x.title===name),related=data.filter(x=>x!==current&&(!current||!current.category||x.category===current.category)).slice(0,3);document.getElementById('relatedGrid').innerHTML=related.map(p=>{const k=p.product_no||p.cas||p.title;return `<article class="product-card success-card"><span class="compound-no">${esc(p.product_no||'Reference standard')}</span><h3>${esc(p.title)}</h3><div class="cas">CAS <b>${esc(p.cas||'Not listed')}</b></div><div class="card-foot"><span class="class-pill">${esc(p.category||'Natural product')}</span><a href="inquiry.html?key=${encodeURIComponent(k)}">Inquire <span>→</span></a></div></article>`}).join('')||'<div class="empty">Browse the catalog for more compounds.</div>'}catch(e){document.getElementById('relatedGrid').innerHTML='<div class="empty">Browse the catalog for more compounds.</div>'}})();
